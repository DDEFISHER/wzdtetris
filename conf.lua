-- Configuration
function love.conf(t)
	t.title = "wzd tetris" -- The title of the window the game is in (string)
	t.window.width = 500        -- we want our game to be long and thin.
	t.window.height = 800

end
